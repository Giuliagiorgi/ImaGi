customtkinter>=5.2.2
pillow>=10.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
torch
torchvision
umap-learn>=0.5.5
# Optional: better clustering, but on Windows it may require build tools.
hdbscan>=0.8.33
# Optional: semantic image embeddings.
open-clip-torch>=2.24.0
